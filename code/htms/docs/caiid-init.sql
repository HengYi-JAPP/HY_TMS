insert into caiid_areaid                       (caiid) select max(AREAID)+1 from TB_HTMS_AREA                  ;
insert into caiid_caddrid                      (caiid) select max(CADDRID)+1 from TB_HTMS_CUSTOMERADDR         ;
insert into caiid_carrierid                    (caiid) select max(CARRIERID)+1 from TB_HTMS_CARRIER            ;
insert into caiid_corderid                     (caiid) select max(CORDERID)+1 from TB_HTMS_CUSTORDER           ;
insert into caiid_customerid                   (caiid) select max(CUSTOMERID)+1 from TB_HTMS_CUSTOMER          ;
insert into caiid_custpriceid                  (caiid) select max(PRICEID)+1 from TB_HTMS_CUSTOMER_PRICE       ;
insert into caiid_driverid                     (caiid) select max(DRIVERID)+1 from TB_HTMS_DRIVER              ;
insert into caiid_factoryid                    (caiid) select max(FACTORYID)+1 from TB_HTMS_FACTORY            ;
insert into caiid_logid                        (caiid) select max()+1 from                                     ;
insert into caiid_logisticsid                  (caiid) select max(LOGISTICSID)+1 from TB_HTMS_LOGISTICS        ;
insert into caiid_priceid                      (caiid) select max(PRICEID)+1 from TB_HTMS_TRANSPRICE           ;
insert into caiid_productid                    (caiid) select max(PRODUCTID)+1 from TB_HTMS_PRODUCT            ;
insert into caiid_routeid                      (caiid) select max(ROUTEID)+1 from TB_HTMS_ROUTE                ;
insert into caiid_senddetailid                 (caiid) select max(SENDDETAILID)+1 from TB_HTMS_SENDDETAIL      ;
insert into caiid_sendid                       (caiid) select max(SENDID)+1 from TB_HTMS_SEND                  ;
insert into caiid_shipdetailid                 (caiid) select max(SHIPDETAILID)+1 from TB_HTMS_SHIPDETAIL      ;
insert into caiid_shipid                       (caiid) select max(SHIPID)+1 from TB_HTMS_SHIP                  ;
insert into caiid_sportdetailid                (caiid) select max(SPORTDETAILID)+1 from TB_HTMS_TRANSPORTDETAIL;
insert into caiid_synid                        (caiid) select max(SYNID)+1 from TB_HTMS_ORDERSYN               ;
insert into caiid_taskid                       (caiid) select max(TASKID)+1 from TB_HTMS_TASKSCHEDULE          ;
insert into caiid_tb_datasource                (caiid) select max(DBSID)+1 from TB_DATASOURCE                  ;
insert into caiid_tb_dictionary                (caiid) select max(DICID)+1 from TB_DICTIONARY                  ;
insert into caiid_tb_dic_type                  (caiid) select max(TYPEID)+1 from TB_DIC_TYPE                   ;
insert into caiid_tb_system                    (caiid) select max(SYSID)+1 from TB_SYSTEM                      ;
insert into caiid_tb_wsm_group                 (caiid) select max(groupid)+1 from tb_wsm_group                 ;
insert into caiid_tb_wsm_infotype              (caiid) select max(typeid)+1 from tb_wsm_infotype               ;
insert into caiid_tb_wsm_organization          (caiid) select max(orgid)+1 from tb_wsm_organization            ;
insert into caiid_tb_wsm_privilege             (caiid) select max(privid)+1 from tb_wsm_privilege              ;
insert into caiid_tb_wsm_role                  (caiid) select max(roleid)+1 from tb_wsm_role                   ;
insert into caiid_tb_wsm_user                  (caiid) select max(userid)+1 from tb_wsm_role                   ;
insert into caiid_transdetailid                (caiid) select max(TRANSDETAILID)+1 from caiid_transdetailid    ;
insert into caiid_transid                      (caiid) select max(TRANSID)+1 from TB_HTMS_TRANSFER             ;
insert into caiid_transportid                  (caiid) select max(TRANSPORTID)+1 from TB_HTMS_TRANSPORT        ;
insert into caiid_typeid                       (caiid) select max(TYPEID)+1 from TB_HTMS_PRODUCTTYPE           ;
insert into caiid_vehicleid                     (caiid) select max(VEHICLEID)+1 from TB_HTMS_VEHICLE            ;